import {style} from '@vanilla-extract/css';

export const itemSetHorizontal = style({
  display: 'flex',
  gap: '2%',
})

export const checkboxStyles = style({})

export const styleForBetweenLabelAndCheckbox = style({
  margin: '0 10px 0 10px'
})